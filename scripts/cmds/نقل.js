const axios = require('axios');
const jimp = require("jimp");
const fs = require("fs")


module.exports = {
    config: {
        name: "لولبوت",
        aliases: ["lol"],
        version: "1.0",
        author: "Taseen",
        countDown: 5,
        role: 0,
        shortDescription: "بوتسلايب شخص ما",
        longDescription: "ترفيه",
        category: "",
        guide: "{pn}"
    },

onStart: async function ({ api, message, event }) {
		
	}

    
  }